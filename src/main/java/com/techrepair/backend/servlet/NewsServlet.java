package com.techrepair.backend.servlet;

/*
 * NewsServlet was previously mapped to `/evidencia-noticias` and opened the
 * news UI in a separate browser window. This servlet is intentionally
 * disabled to keep the news UI inside the main application interface and to
 * prefer the REST API endpoints under `/api/news`. If you need to remove the
 * file entirely, you can delete this class.
 */
public class NewsServlet {
    // Servlet disabled: intentionally left empty.
}
